@model AddMovieVM
@{
    Layout = null;
}

<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>AddMovie</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" />
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/5.1.3/css/bootstrap.min.css" />
    <link href="~/css/Movie/AddMovie.css" rel="stylesheet" />
</head>
<body>
    <div class="container">
        <h2 class="text-center">🎬 Add New Movie</h2>

        <div class="form-container">
            <form asp-action="Add" method="post" enctype="multipart/form-data">
                <div class="mb-3">
                    <label asp-for="Title" class="form-label">Title</label>
                    <input asp-for="Title" class="form-control" placeholder="Enter movie title" />
                    <span asp-validation-for="Title" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="Description" class="form-label">Description</label>
                    <textarea asp-for="Description" class="form-control" rows="4" placeholder="Enter movie description"></textarea>
                    <span asp-validation-for="Description" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="ReleaseYear" class="form-label">Release Year</label>
                    <input asp-for="ReleaseYear" class="form-control" type="number" placeholder="Enter release year" />
                    <span asp-validation-for="ReleaseYear" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="Duration" class="form-label">Duration (minutes)</label>
                    <input asp-for="Duration" class="form-control" type="number" placeholder="Enter duration" />
                    <span asp-validation-for="Duration" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <label class="form-label">Genres</label>
                    <select class="form-control" name="SelectedGenres" multiple>
                        @foreach (var genre in Model.GenreList)
                        {
                            <option value="@genre.Value">@genre.Text</option>
                        }
                    </select>
                    <span asp-validation-for="SelectedGenres" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="PosterUrl" class="form-label">Poster URL</label>
                    <input asp-for="PosterUrl" class="form-control" type="url" placeholder="Enter poster URL" />
                    <span asp-validation-for="PosterUrl" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="TrailerUrl" class="form-label">Trailer URL</label>
                    <input asp-for="TrailerUrl" class="form-control" type="url" placeholder="Enter trailer URL" />
                    <span asp-validation-for="TrailerUrl" class="text-danger"></span>
                </div>

                <button type="submit" class="btn btn-success">
                    <i class="fas fa-plus-circle"></i> Add Movie
                </button>
            </form>
        </div>
    </div>

    @section Scripts {
        <partial name="_ValidationScriptsPartial" />
        <a href="c:\users\admin\source\repos\netolewx\netolewx\views\admin\add.cshtml">c:\users\admin\source\repos\netolewx\netolewx\views\admin\add.cshtml</a>
    }
</body>
</html>
<style>

    /* Modern Enhanced Movie Form Styles - Dark Theme */
:root {
    --primary-gradient: linear-gradient(135deg, #1a1a1a, #333333); /* Dark gradient */
    --form-bg: rgba(34, 34, 34, 0.95); /* Dark form background */
    --input-bg: rgba(51, 51, 51, 0.9); /* Dark input background */
    --shadow-color: rgba(0, 0, 0, 0.3); /* Darker shadow */
    --primary-text: #e0e0e0; /* Light text */
    --secondary-text: #a0a0a0; /* Lighter secondary text */
    --button-gradient: linear-gradient(135deg, #4CAF50, #2E8B57); /* Green gradient */
    --button-hover: linear-gradient(135deg, #2E8B57, #1E6847); /* Darker green gradient */
    --border-color: #444444; /* Dark border */
    --focus-ring: rgba(66, 153, 225, 0.4); /* Blue focus ring */
    --error-color: #ff6b6b; /* Red error color */
}

body {
    background: var(--primary-gradient);
    font-family: 'Poppins', sans-serif;
    margin: 0;
    padding: 1rem;
    min-height: 100vh;
    color: var(--primary-text);
    line-height: 1.6;
    display: flex;
    justify-content: center;
    align-items: center;
}

.container {
    width: 90%;
    max-width: 1100px;
    margin: 0 auto;
    padding: 1rem;
}

h2 {
    font-size: 2.8rem;
    font-weight: 700;
    text-align: center;
    color: var(--primary-text);
    margin-bottom: 2rem;
}

.form-container {
    background: var(--form-bg);
    border-radius: 1rem;
    box-shadow: 0 8px 24px var(--shadow-color);
    padding: 2.5rem;
    backdrop-filter: blur(12px);
    border: 1px solid var(--border-color);
    width: 100%;
}

.form-label {
    display: block;
    font-weight: 600;
    color: var(--secondary-text);
    margin-bottom: 0.6rem;
    font-size: 1rem;
}

.form-control {
    width: 100%;
    padding: 0.85rem;
    font-size: 1rem;
    border: 1px solid var(--border-color);
    border-radius: 0.5rem;
    background: var(--input-bg);
    transition: all 0.3s ease;
    color: var(--primary-text);
}

    .form-control:focus {
        outline: none;
        border-color: #3182ce;
        box-shadow: 0 0 0 4px var(--focus-ring);
        background: #444444; /* Slightly lighter background on focus */
    }

.btn-submit {
    display: inline-block;
    background: var(--button-gradient);
    color: white;
    padding: 0.9rem 1.5rem;
    border: none;
    border-radius: 0.5rem;
    font-size: 1.1rem;
    cursor: pointer;
    transition: background 0.3s ease, transform 0.2s;
}

    .btn-submit:hover {
        background: var(--button-hover);
        transform: translateY(-2px);
    }

    .btn-submit:active {
        transform: translateY(1px);
    }

.btn-success {
    background: linear-gradient(135deg, #28a745, #218838);
    border: none;
    color: white;
    padding: 12px 20px;
    font-size: 16px;
    font-weight: 600;
    border-radius: 8px;
    cursor: pointer;
    transition: all 0.3s ease;
    width: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
}

    .btn-success:hover {
        background: linear-gradient(135deg, #218838, #1e7e34);
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(40, 167, 69, 0.3);
    }

    .btn-success:active {
        transform: translateY(0);
        box-shadow: 0 3px 10px rgba(40, 167, 69, 0.3);
    }

.error-message {
    color: var(--error-color);
    font-size: 0.9rem;
    margin-top: 0.4rem;
    display: block;
}


</style>